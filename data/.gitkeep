# This file exists so git tracks the data/ folder structure.
# The actual data files are NOT committed to git because they are too large.
#
# Run setup.sh (Mac/Linux) or setup.ps1 (Windows) to download them automatically.
